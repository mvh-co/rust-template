// Auto-generated types from AsyncAPI specification
// DO NOT EDIT MANUALLY

use serde::{Serialize, Deserialize};
use std::collections::HashMap;

{{#each (getAllSchemas asyncapi)}}
{{#with (lookup ../asyncapi.components.messages @key)}}
{{#if this.payload}}
{{generateDoc this.payload.description}}
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
pub struct {{@key | toPascalCase}} {
    {{#each this.payload.properties}}
    {{generateDoc this.description}}
    pub {{@key | toSnakeCase}}: {{mapType this.type this.format this}},
    {{/each}}
}
{{/if}}
{{/with}}
{{/each}}

/// Generic message envelope for AsyncAPI
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
pub struct MessageEnvelope {
    #[serde(flatten)]
    pub payload: HashMap<String, serde_json::Value>,
}