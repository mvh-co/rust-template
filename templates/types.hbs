// Auto-generated types from AsyncAPI specification
// DO NOT EDIT MANUALLY

use serde::{Serialize, Deserialize};
use std::collections::HashMap;

{{#each asyncapi.components.messages as |message messageKey|}}
{{#with (resolveMessagePayload @root.asyncapi message.payload)}}
{{#if additionalProperties}}
{{generateDoc description}}
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
pub struct {{messageKey | toPascalCase}}(pub serde_json::Value);
{{else}}
{{generateDoc description}}
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
pub struct {{messageKey | toPascalCase}} {
    {{#each properties}}
    {{generateDoc this.description}}
    {{#if (isConst this)}}
    #[serde(default)]
    pub {{@key | toSnakeCase}}: {{mapType this.type this.format this}},
    {{else if (isRequired ../required @key)}}
    pub {{@key | toSnakeCase}}: {{mapType this.type this.format this}},
    {{else}}
    #[serde(skip_serializing_if = "Option::is_none")]
    pub {{@key | toSnakeCase}}: Option<{{mapType this.type this.format this}}>,
    {{/if}}
    {{/each}}
}
{{/if}}
{{/with}}
{{/each}}

/// Generic message envelope for AsyncAPI
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
pub struct MessageEnvelope {
    #[serde(flatten)]
    pub payload: HashMap<String, serde_json::Value>,
}