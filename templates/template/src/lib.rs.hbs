//! {{ asyncapi._json.info.title }} WebSocket SDK
//!
//! Generic Rust client generated from AsyncAPI specification (v{{ asyncapi._json.info.version }}).
//! This SDK provides a WebSocket client for the API described in the specification.
//!
//! ## Example
//!
{% set selectedServer = asyncapi._json.servers[server] %}
//! ```rust,no_run
//! use {{ asyncapi._json.info.title | toSnakeCase }}_sdk::ws::client::AsyncApiClient;
//!
//! #[tokio::main]
//! async fn main() {
//!     let url = {% if selectedServer.url %}"{{ selectedServer.url }}"{% else %}"{{ selectedServer.protocol or 'ws' }}://{{ selectedServer.host }}{{ selectedServer.pathname or '' }}"{% endif %};
//!     let mut client = AsyncApiClient::new(url);
//!     client.connect().await.unwrap();
//!     // Use client methods here
//! }
//! ```

pub mod ws;